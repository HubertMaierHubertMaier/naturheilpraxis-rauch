# Infothek source pages

These HTML files are source material for the separate public website. They are
not copied into the Lovable app build. Keep medical pages `noindex` until their
content, reviewer information, canonical URL and redirect mapping are approved.

Patient-only pages are also stored here as source, but the app serves them only
through the authorized `get-infothek-html` Edge Function and private storage.
